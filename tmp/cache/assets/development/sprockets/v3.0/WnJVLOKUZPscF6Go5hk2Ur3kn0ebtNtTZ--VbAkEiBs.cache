I"£
/*
Phrase Shuffler React Component
Released under the MIT License
Date: 11-02-2015
Author: Julio Cesar Fausto
Source: https://github.com/jcfausto/react-phrase-shuffler-component
 */

(function() {
  this.PhraseShuffler = React.createClass({
    getInitialState: function() {
      return {
        phrases: this.props.phrases,
        phrase_count: this.props.phrases.lenght,
        lastIndex: -1
      };
    },
    nextIndex: function() {
      this.state.lastIndex += 1;
      if (this.state.lastIndex >= this.state.phrases_count) {
        this.state.lastIndex = 0;
      }
      return this.state.lastIndex;
    },
    fadeOut: function() {
      var titleElement;
      titleElement = document.querySelector('.hero h2:last-child');
      titleElement.style.opacity = 0;
      return setTimeout(this.fadeIn, 250);
    },
    fadeIn: function() {
      var titleElement;
      titleElement = document.querySelector('.hero h2:last-child');
      titleElement.innerHTML = this.state.phrases[this.nextIndex()];
      titleElement.style.opacity = 1;
      return setTimeout(this.fadeOut, 2750);
    },
    shuffle: function() {
      var j, phrases_count, results, x;
      phrases_count = this.state.phrases_count;
      results = [];
      while (phrases_count) {
        j = Math.floor(Math.random() * phrases_count);
        x = this.state.phrases[--phrases_count];
        this.state.phrases[phrases_count] = this.state.phrases[j];
        results.push(this.state.phrases[j] = x);
      }
      return results;
    },
    start: function() {
      this.shuffle();
      return this.fadeIn();
    },
    render: function() {
      this.start();
      return false;
    }
  });

}).call(this);
:ET