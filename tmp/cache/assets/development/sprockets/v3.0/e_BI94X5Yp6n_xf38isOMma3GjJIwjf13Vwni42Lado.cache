I"#
/*
Phrase Shuffler React Component
Released under the MIT License
Date: 11-02-2015
Author: Julio Cesar Fausto
Source: https://github.com/jcfausto/react-phrase-shuffler-component
 */

(function() {
  this.PhraseShuffler = React.createClass({
    getInitialState: function() {
      return {
        phrases: this.props.phrases,
        current_phrase: "Shuffler",
        lastIndex: -1
      };
    },
    nextIndex: function() {
      this.state.lastIndex += 1;
      if (this.state.lastIndex >= this.state.phrases.length) {
        this.state.lastIndex = 0;
      }
      return this.state.lastIndex;
    },
    shuffle: function() {
      this.state.current_phrase = this.state.phrases[this.nextIndex()];
      return console.log(this.state.current_phrase);
    },
    fadeOut: function(titleElement) {
      titleElement.style.opacity = 0;
      return setTimeout(alert("ho"), 250);
    },
    fadeIn: function(titleElement) {
      titleElement.innerHTML = this.state.phrases[this.nextIndex()];
      titleElement.style.opacity = 1;
      return setTimeout(alert("hi"), 2750);
    },
    render: function() {
      var i, j, titleElement, x;
      titleElement = document.querySelector('.hero h2:last-child');
      this.state.lastIndex = -1;
      i = this.state.phrases.length;
      while (i) {
        j = Math.floor(Math.random() * i);
        x = this.state.phrases[--i];
        this.state.phrases[i] = this.state.phrases[j];
        this.state.phrases[j] = x;
      }
      this.fadeIn(titleElement);
      return <h1>teste</h1>;
    }
  });

}).call(this);
:ET