<%= form_for(@hero_phrase) do |f| %>
  <% if @hero_phrase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hero_phrase.errors.count, "error") %> prohibited this hero_phrase from being saved:</h2>

      <ul>
      <% @hero_phrase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
